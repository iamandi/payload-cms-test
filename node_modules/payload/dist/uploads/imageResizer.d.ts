import { CollectionConfig } from '../collections/config/types';
import { FileSizes } from './types';
/**
 * @description
 * @param staticPath Path to save images
 * @param config Payload config
 * @param savedFilename
 * @param mimeType
 * @returns image sizes keyed to strings
 */
export default function resizeAndSave(staticPath: string, config: CollectionConfig, savedFilename: string, mimeType: string): Promise<FileSizes>;
