import { PayloadRequest } from '../../express/types';
import { User } from '../types';
export declare type Result = {
    user?: User;
    collection?: string;
    token?: string;
    exp?: string;
};
export declare type Arguments = {
    req: PayloadRequest;
};
declare function me({ req }: Arguments): Promise<Result>;
export default me;
