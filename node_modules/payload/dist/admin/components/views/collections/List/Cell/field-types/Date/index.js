import React from 'react';
import format from 'date-fns/format';
const DateCell = ({ data }) => (React.createElement("span", null, data && format(new Date(data), 'MMMM do yyyy, h:mm a')));
export default DateCell;
