import { FileSizes } from './types';
declare const getThumbnail: (mimeType: string, staticURL: string, filename: string, sizes: FileSizes, adminThumbnail: string) => string | boolean;
export default getThumbnail;
