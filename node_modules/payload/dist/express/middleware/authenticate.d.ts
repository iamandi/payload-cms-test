import { Request, Response, NextFunction } from 'express';
import { Config } from '../../config/types';
export declare type PayloadAuthenticate = (req: Request, res: Response, next: NextFunction) => NextFunction;
declare const _default: (config: Config) => PayloadAuthenticate;
export default _default;
