import { Field, HookName } from './config/types';
declare type Arguments = {
    hook: HookName;
    field: Field;
    path: string;
    errors: {
        message: string;
        field: string;
    }[];
    newData: Record<string, unknown>;
    existingData: Record<string, unknown>;
};
declare const validationPromise: ({ errors, hook, newData, existingData, field, path, }: Arguments) => Promise<string | boolean>;
export default validationPromise;
