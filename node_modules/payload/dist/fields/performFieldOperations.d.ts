import { CollectionConfig } from '../collections/config/types';
import { GlobalConfig } from '../globals/config/types';
import { Operation } from '../types';
import { PayloadRequest } from '../express/types';
import { HookName } from './config/types';
declare type Arguments = {
    data: Record<string, unknown>;
    operation: Operation;
    hook: HookName;
    req: PayloadRequest;
    overrideAccess: boolean;
    reduceLocales?: boolean;
    originalDoc?: Record<string, unknown>;
    id?: string;
    showHiddenFields?: boolean;
    depth?: number;
    currentDepth?: number;
};
export default function performFieldOperations(entityConfig: CollectionConfig | GlobalConfig, args: Arguments): Promise<{
    [key: string]: unknown;
}>;
export {};
