import { CollectionConfig, PayloadCollectionConfig } from './types';
declare const sanitizeCollection: (collections: PayloadCollectionConfig[], collection: PayloadCollectionConfig) => CollectionConfig;
export default sanitizeCollection;
