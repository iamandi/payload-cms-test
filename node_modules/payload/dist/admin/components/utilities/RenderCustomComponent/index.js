import React from 'react';
const RenderCustomComponent = (props) => {
    const { CustomComponent, DefaultComponent, componentProps } = props;
    if (CustomComponent) {
        return (React.createElement(CustomComponent, Object.assign({}, componentProps)));
    }
    return (React.createElement(DefaultComponent, Object.assign({}, componentProps)));
};
export default RenderCustomComponent;
